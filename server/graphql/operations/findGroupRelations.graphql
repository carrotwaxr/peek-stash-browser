query FindGroupRelations($filter: FindFilterType) {
  findGroups(filter: $filter) {
    count
    groups {
      id
      sub_groups {
        group {
          id
        }
        description
      }
    }
  }
}
