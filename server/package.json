{
  "name": "stash-player-server",
  "version": "1.0.0",
  "description": "Backend for stash-player. Handles serving the client, API endpoints, communication with media library manager (Stash), and transcoding",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "node dist/index.js",
    "start": "node dist/index.js",
    "dev": "nodemon --watch './**/*.ts' --exec 'node --loader ts-node/esm' index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "nodemon": "^3.1.10",
    "prisma": "^6.17.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.17.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "graphql-tag": "^2.12.6",
    "stashapp-api": "^0.2.5"
  }
}
